<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <div class="container">
        <div class="header">
            <h1>Daftar Menu</h1>
        </div>

        <div class="menu-section">
            <h2>Makanan</h2>
            <ul class="menu-list">
                <% for( let index = 0; index < makanan.length; index++ ) { %>
                    <li><span><%= makanan[index].nama_menu%></span><span><%= makanan[index].harga_menu %></span></li>
                <% } %>
            </ul>
        </div>

        <div class="menu-section">
            <h2>Minuman</h2>
            <ul class="menu-list">
                <% for( let index = 0; index < minuman.length; index++ ) { %>
                    <li><span><%= minuman[index].nama_menu%></span><span><%= makanan[index].harga_menu %></span></li>
                <% } %>
            </ul>
        </div>

        <div class="contact">
            <p><strong>Pesan Antar:</strong></p>
            <p>+123-695-4311</p>
            <p><a href="#">@Ambatukan</a></p>
        </div>

    </div>
</body>
</html>